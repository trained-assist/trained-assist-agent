# Цели и скоуп продукта

Источник истины для гейта релевантности стадии «Фиксер» (issue → PR,
`src/issue-fixer.js`, см. ISSUES-TO-PR-SPEC.md в рабочем проекте владельца).
Фиксер читает этот файл, а не хардкод в промпте: чтобы сузить или расширить то,
что чинится автоматически, редактируй ЭТОТ файл, а не код.

**Статус: черновой каркас.** Владелец расширяет цели/сценарии/out-of-scope
по мере необходимости — гейт читает файл целиком при каждом запуске.

## Цели

- Рекрутер проходит путь «вакансия → горячие отклики → оффер» без ручных обходов.
- Заявки пользователей (баги/фичи) из `/bug_or_feature` доходят до GitHub issue
  без ручной работы (см. `BUGS-AND-FEATURES-SPEC.md`).
- Локальные, однозначные баги в существующих сценариях чинятся без участия
  человека до стадии review PR.

## В скоупе (сценарии)

- `docs/user-scenarios/recruiter/*.md` — сценарии домена «рекрутер».
- `docs/user-scenarios/engineering/*.md` — сценарии инженерной разработки через
  Playbooks (#1372, `playbooks/development.json`).
- `docs/user-scenarios/exhibition/*.md` — сценарии обработки участников выставок
  (каталог → сайт продаж).
- `docs/user-scenarios/freelance/*.md` — сценарии спецификации фриланс-проекта
  (intake → GO/NO-GO).
- Любой новый `docs/user-scenarios/<domain>/*.md`, добавленный по тому же формату
  (см. README.md в этой папке).

## Вне скоупа (НЕ чиним автоматически)

- Интеграции и продукты, которых нет ни в одном сценарии выше.
- Запросы вида «сделайте мне X» (новый продукт/фича без привязки к существующему
  сценарию) — это не баг в сценарии, а новая фича; отдаётся человеку.
- Изменения архитектуры: эпики, `size:XL`, всё с лейблом `needs-architect`.
- Всё с `ambiguity:high` — фиксер не угадывает, что имел в виду автор issue.
- Правки веб-UI, миграции старых issue, мерж/автомерж PR — см. §7
  ISSUES-TO-PR-SPEC.md.
