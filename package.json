{
  "name": "trained-assist-agent",
  "version": "0.1.0",
  "description": "Trained Assist Agent \u2014 Claude Code session runner",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js src/runner.js src/session-store.js src/secrets.js src/browser.js",
    "test": "vitest run && npm run test:cjs",
    "test:cjs": "node test/answer-router.test.cjs && node test/gtd-controller.test.cjs && node test/session-context-batch.test.cjs && node test/final-text-select.test.cjs && node test/session-store-recency.test.cjs && node test/lane-granularity.test.cjs && node test/cap-per-profile.test.cjs",
    "test:watch": "vitest",
    "test:staging": "node scripts/staging/run.mjs"
  },
  "devDependencies": {
    "nock": "^14.0.17",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "@playwright/mcp": "^0.0.29",
    "axios": "^1.7.0",
    "better-sqlite3": "^11.0.0",
    "googleapis": "^176.0.0",
    "marked": "^18.0.11",
    "playwright": "^1.50.0"
  }
}
