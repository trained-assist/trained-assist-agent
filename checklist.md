Goal: P3d-1 of #1426 — validator registry + programmatic step execution + validation/evidence recording (PR #1429)

- [x] CI green on https://github.com/trained-assist/trained-assist-agent/pull/1429
- [x] Merged to main
- [x] Deployed to prod — verified live

Goal: fix(deploy) — provision trained-assist-engineering sibling checkout so dev_workspace_setup / engineering_spawn_workspace don't break prod on every fresh release (#1418, #1434)

- [ ] CI green on https://github.com/trained-assist/trained-assist-agent/pull/1434
- [ ] Merged to main
- [ ] Deployed to prod — verified live (re-check dev_workspace_setup end-to-end after this deploy, not just /health commit SHA — the manual on-VM symlink fix already masks the bug until the next fresh VM/release)
